# REQUIRED ENVIRONMENT VARIABLES FOR SUPABASE AUTH
# =========================================================

# Supabase Project URL (Find in: Supabase Dashboard > Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key (Find in: Supabase Dashboard > Project Settings > API > Project API keys > anon public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# OPTIONAL API KEYS (Required only if you enable the specific API)
# =========================================================

# OpenAI API Key (Find in: https://platform.openai.com/account/api-keys)
OPENAI_API_KEY=sk-your-openai-key

# Anthropic API Key (Find in: https://console.anthropic.com/account/keys)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Replicate API Token (Find in: https://replicate.com/account/api-tokens)
REPLICATE_API_TOKEN=your-replicate-token

# Stripe API Keys (Find in: https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret

# Resend API Key (Find in: https://resend.com/api-keys)
RESEND_API_KEY=re_your-resend-key

# Deepgram API Key (Find in: https://console.deepgram.com/account)
DEEPGRAM_API_KEY=your-deepgram-key

# MediaWiki/Wikipedia API (No key required for public access, but might need for higher rate limits)
# MEDIAWIKI_API_KEY=your-mediawiki-key

# React PDF License Key (If using commercial license)
# REACT_PDF_LICENSE_KEY=your-react-pdf-license-key

# NEXT.JS ENVIRONMENT VARIABLES
# =========================================================
# Needed to set up absolute imports and other configurations
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Production URL (for production builds)
# NEXT_PUBLIC_PRODUCTION_URL=https://your-production-domain.com

# NODE ENVIRONMENT
# =========================================================
# Set to 'development' for local development, 'production' for production builds
NODE_ENV=development

# SECURITY VARIABLES
# =========================================================
# Random string for encryption (generate with: openssl rand -base64 32)
AUTH_SECRET=your-auth-secret 